{
  "name": "thenextopportunityfinder",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "build": "cd web && npm install && npm run build && node -e \"import('fs').then(fs => { fs.mkdirSync('../dist', {recursive: true}); fs.mkdirSync('../public', {recursive: true}); fs.cpSync('dist', '../dist', {recursive: true}); fs.cpSync('dist', '../public', {recursive: true}); fs.copyFileSync('dist/index.html', '../index.html'); })\"",
    "dev": "cd web && npm run dev"
  }
}
