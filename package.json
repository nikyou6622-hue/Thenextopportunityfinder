{
  "name": "thenextopportunityfinder",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "build": "cd web && npm install && npm run build && node -e \"require('fs').cpSync('dist', '../dist', {recursive: true}); require('fs').cpSync('dist', '../public', {recursive: true}); require('fs').copyFileSync('dist/index.html', '../index.html')\"",
    "dev": "cd web && npm run dev"
  }
}
